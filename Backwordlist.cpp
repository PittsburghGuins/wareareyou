#include <iostream>
#include <array>
int main()
 {
    int a[3];
    std::getline(std::cin, a[3]);

    for(int i = 1; i <= input.length(); i++)
    {
        std::cout << input[input.length() - i];
    }

    std::cout << '\n';
    return 0;
}