#include <iostream>
#include <string>

class Book{
    public:
    def __init__(self,title: str, author: str);
        // add code to set title and author
        self.availability = True // book is available by default
        
    def __str__(self) -> str:
        //return a string that briefly describes the book (title, author)
        return " ";
        
    def addsummary(self,summary: str);
        // add a summary to the book
        return 0;
        
    def describe(self) -> str;
        // return a formatted string including title, author, and summary
        return 0;
}